import { PageHeading } from "@/components/page-heading";
import { Card } from "@/components/ui/card";
import { projects } from "@/lib/data";

export default function ProjectsPage() { return <><PageHeading eyebrow="Workspaces" title="Project Intelligence" description="See health, commitments, decisions, dependencies, blockers, and project history in one operating view." /><div className="grid gap-4 lg:grid-cols-3">{projects.map((project) => <Card key={project.name} className="p-5"><div className="flex items-start justify-between"><div><p className="text-sm font-medium">{project.name}</p><p className="mt-1 text-[11px] text-zinc-600">Updated {project.activity}</p></div><span className="text-xl font-semibold">{project.health}%</span></div><div className="mt-5 h-1.5 rounded-full bg-white/5"><div className="h-full rounded-full bg-gradient-to-r from-violet-500 to-cyan-400" style={{ width: `${project.health}%` }} /></div><div className="mt-5 grid grid-cols-3 gap-2 text-center"><div className="rounded-lg bg-white/[.03] p-2"><p className="text-sm">{project.risks}</p><p className="text-[9px] text-zinc-600">risks</p></div><div className="rounded-lg bg-white/[.03] p-2"><p className="text-sm">8</p><p className="text-[9px] text-zinc-600">tasks</p></div><div className="rounded-lg bg-white/[.03] p-2"><p className="text-sm">5</p><p className="text-[9px] text-zinc-600">decisions</p></div></div></Card>)}</div></>; }
