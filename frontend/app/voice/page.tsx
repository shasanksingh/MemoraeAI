import { PageHeading } from "@/components/page-heading";
import { Card } from "@/components/ui/card";
import { AudioLines, CloudUpload, Mic } from "lucide-react";

export default function VoicePage() { return <><PageHeading eyebrow="Capture" title="Voice Intelligence" description="Turn voice notes and calls into searchable commitments, decisions, reminders, and graph updates—using hosted transcription only." /><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr]"><Card className="grid min-h-[360px] place-items-center border-dashed p-8 text-center"><div><div className="mx-auto grid size-16 place-items-center rounded-2xl bg-violet-400/10"><Mic className="size-7 text-violet-400" /></div><p className="mt-5 text-sm font-medium">Record or upload a voice note</p><p className="mx-auto mt-2 max-w-xs text-xs leading-5 text-zinc-600">MP3, WAV, M4A, or a supplied transcript. Audio is sent directly to your configured provider.</p><button className="mt-6 inline-flex items-center gap-2 rounded-xl bg-violet-500 px-4 py-2.5 text-xs font-medium"><CloudUpload className="size-3.5" />Choose audio</button></div></Card><Card className="p-5"><div className="flex items-center gap-3"><AudioLines className="size-4 text-cyan-400"/><p className="text-sm">Recent voice intelligence</p></div><div className="mt-5 space-y-3">{["Team retro notes", "Call with Nina", "Idea: graph-based daily brief"].map((name, i) => <div key={name} className="rounded-xl border border-white/[.06] bg-black/10 p-4"><div className="flex justify-between"><p className="text-xs text-zinc-200">{name}</p><span className="text-[10px] text-zinc-600">{i + 1}d ago</span></div><p className="mt-2 text-[11px] text-zinc-600">{3-i} actions · {i+1} decisions · transcript ready</p></div>)}</div></Card></div></>; }
